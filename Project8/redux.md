# Redux is an independent statemanagement library
  --> To use redux in react we need "react-redux"
  --> in redux the data flow is better than context API
## MAIN COMPONENTS OF REDUX TOOLKIT
  
  # STORE --> global variable type of thing in we keep variable as single source of thruth

  # REDUCERS --> if you want to changes the store  --they are object can be understood using example
  # useSelector --> method to selevct value in store
  # useDispatch --> method to send variable from store
   


